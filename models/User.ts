/**
 * Represents a user with credentials and optional personal information.
 */
/**
 * Represents a user with credentials and optional personal information.
 */
export interface User {
  username: string;
  password: string;
  firstName?: string;
  lastName?: string;
}